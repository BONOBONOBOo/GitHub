X>>>Y 왼쪽 비트는 항상 0으로 채움, 결과는 항상 양수

(조건식 표현)?true일떄 수행될 표현식 : flase일떄 수용될 표현식

*연상식내에서 데이터 타입들이 다른 경우, 결과 데이터 타입중에서 가장 큰 타입으로 자동으로 프로모션이 일어나기 때문에 저장될 변수의 타입을 맞게 선언하거나 캐스팅해서 처리해야한다.



//byte,short,int,char,String
switch(표현식의 리턴)

배열은 reference Type 객체로 취급합니다.
new 키워드로 생성후 heap 메모리에 저장합니다.

배열은 타입에 따라서 요소값을 기본값으로 저장합니다
배열은 객체로 생성되면 자동으로 길이속성에 요소수를 저장합니다.